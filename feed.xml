---
layout: null
permalink: /feed.xml
---
<?xml version="1.0" encoding="UTF-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>{{ site.title | xml_escape }}</title>
  <subtitle>{{ site.description | xml_escape }}</subtitle>
  <link href="{{ '/feed.xml' | absolute_url }}" rel="self" type="application/atom+xml"/>
  <link href="{{ site.url | xml_escape }}" rel="alternate" type="text/html"/>
  <updated>{{ site.time | date_to_xmlschema }}</updated>
  <id>{{ site.url | xml_escape }}/</id>
  <author>
    <name>{{ site.author | xml_escape }}</name>
  </author>
  {% for post in site.posts limit:20 %}
  <entry>
    <title>{{ post.title | xml_escape }}</title>
    <link href="{{ post.url | absolute_url }}" rel="alternate" type="text/html"/>
    <id>{{ post.url | absolute_url }}</id>
    <published>{{ post.date | date_to_xmlschema }}</published>
    <updated>{{ post.last_modified_at | default: post.date | date_to_xmlschema }}</updated>
    <summary>{{ post.excerpt | strip_html | normalize_whitespace | truncate: 300 | xml_escape }}</summary>
    {% for cat in post.categories %}
    <category term="{{ cat | xml_escape }}"/>
    {% endfor %}
    {% for tag in post.tags %}
    <category term="{{ tag | xml_escape }}" label="{{ tag | xml_escape }}"/>
    {% endfor %}
    {% if post.source_url %}
    <link href="{{ post.source_url | xml_escape }}" rel="related" type="text/html" title="{{ post.publisher | xml_escape }}"/>
    {% endif %}
  </entry>
  {% endfor %}
</feed>
